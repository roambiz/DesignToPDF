# 开发日志

记录从立项做到 **1.2.0** 实际碰到的问题，以及当时怎么处理。给后来改代码、或对照「用户又遇到同样现象」用。

产品名：设计底稿导出。仓库名：DesignToPDF。制作：[lazysci.com](https://lazysci.com) 懒研科技。

---

## 目标（一直没变）

给装不了 AI / PS 的旧电脑看稿。拖入设计文件，导出 PDF 或图片预览底稿。同事之间常用微信传一个 exe，对方是 Win7 或 Win10，多数人不装 Python。

所以发布形态必须是：**一个 64 位 exe，Win7 SP1 到 Win11 都能开**。

---

## 1.0.0 之前到定稿

### 从命令行变成窗口

先做了目录批量转换，旧电脑同事不会敲命令。改成 Qt 拖拽窗口：选输出目录、看日志、后台转换。

### 按页分割只对一种格式有效

后来要 PDF、PNG、JPEG 都能拆页。不勾选时：PDF 保留全部页，图片只出第 1 页。勾选后一页一个文件。

### 使用说明漏了两个选项

窗口上有「覆盖已有文件」「完成后打开」，说明里没写，用户不知道默认会跳过已有结果。1.0.0 补进说明和悬停提示。

### 带文件夹的包，微信只发得出 exe

早期 PyInstaller 是「exe + `_internal`」。微信、网盘经常只传 exe，对方打开就缺库。改成 **单文件**，发给别人只发这一个文件。

### Win7 提示缺少 `api-ms-win-core-path-l1-1-0.dll`

用较新的 Python（3.12）和 PySide6 打的包，在 Win7 上会缺这套 UCRT。发布环境改成 **Python 3.8.10 + PySide2（Qt 5）**，虚拟环境固定为 `.venv38`。不要用 `.venv`（3.12 / PySide6）打给 Win7。

### 启动报 `No module named 'pkg_resources.extern'`

setuptools 70 拆掉了 `pkg_resources.extern`，PyInstaller 的 `pyi_rth_pkgres` 一跑就崩。本程序不用 setuptools 运行时。spec 里排除 `pkg_resources` / `setuptools`，并去掉该 runtime hook。

### 点「打开输出文件夹」把已经失效的路径又建回来

输出目录来自微信缓存或已删除位置时，打开逻辑里 `mkdir` 会把空目录重新建出来。改为：只打开确实存在的文件夹，失效路径从记忆里丢掉。

### 点了「停止」仍提示「导出结束」

停止后仍走成功收尾：弹失败框、「完成后打开」也会触发。改为区分「已停止」，停止时不弹窗、不自动打开。

### 关窗口时转换线程还在跑

线程对象没及时放开，关窗过早会卡住或警告不清晰。关窗会等当前任务告一段落；超时则提示稍后再关。停止只在**两个文件之间**生效，一个很大的 PSD 不会瞬间中断。

### 选项记不住 / 记到坏路径

输出路径、覆盖选项用 `QSettings` 记住。已不存在的输出路径不再恢复。

### 图标发黑、像没换

资源管理器按旧路径缓存图标。打包打到新目录，并用多尺寸 ico。不要用事后改 exe 图标的脚本覆盖。

### 不要在微信里直接打开程序

单文件 exe 每次会解压到临时目录。聊天里点「打开」走的是微信缓存，杀毒扫描也慢，还容易解压失败。约定：**先保存到桌面再双击**。

---

## 1.1.0

### 有的 Win7 从双击到窗口很慢

窗口启动时同步 `import convert`，会拉起 PIL、PyMuPDF、svglib，旧机器上等于没见到窗口就开始加载。改为窗口先出来，转换库延迟加载；拖入文件只做轻量扫描。窗口出现后在后台预热，减轻第一次点「导出」的停顿。单文件第一次解压仍然会慢，属正常。

### 导出后再加文件，会跳过或失败

上一轮成功的文件还留在队列里。再导出时旧文件重跑：结果已在就「跳过」，微信临时文件没了就「失败」。改为完成 / 跳过 / 失败都移出队列；源文件已消失也记失败并清掉，不再悄悄丢掉导致队列卡死。导出结束后立刻可以再拖文件。

### 「完成后打开」打开了错误位置

Win7 上 `QDesktopServices.openUrl(QUrl.fromLocalFile(...))` 对中文路径不可靠。`QPushButton.clicked` 还会传入 `bool`，可能被当成路径。打开逻辑改为 `ShellExecuteW` / `os.startfile`，并且只打开**这一次**的输出文件夹。按钮信号用无参连接，忽略 `clicked(bool)`。

### 中文路径、中文文件名要不要做「编码加强」

本机用中文目录和中文文件名实测：扫描、导出 PDF/PNG、按页拆分、PyMuPDF 打开结果都正常。程序已走 Python 3.8 + `pathlib` + Windows 宽字符 API。自行 GBK 转换或一律改 8.3 短路径，更容易把本来正确的路径弄乱，**1.1.0 没有加这类转换**。

### 「打开输出文件夹」看不出能不能点

文件夹真实存在时，按钮改成浅绿（与进度条同色）。路径无效或被删则回到普通白底。不改变原来的点击行为。

### 路径和勾选每次都记住，想变成可选项

`QSettings` 无开关，浏览和导出都会写入。增加「记住设置」，**默认勾选**（和原来一样）。去掉勾选则清掉已存记录，下次按出厂状态打开；当前窗口里正在用的内容不清空。开关本身始终记住，否则下次又变回默认勾选。

### 从微信直接拖文件，输出记到缓存目录

输入框为空时，默认填「第一个文件旁边的 `\导出结果`」。从聊天拖入时，文件往往在微信 `FileStorage\Temp` 或系统临时目录，微信一清缓存路径就没了，再导出就跳过/失败。防御（不影响桌面、客户文件夹的正常拖入）：

- 源文件在聊天缓存或系统临时目录 → 输出改到 **桌面\导出结果**，日志写一句说明
- 这类路径不写入「记住设置」
- 导出时若输出仍在临时位置，改到桌面，不中断任务
- 浏览文件夹时，若当前是临时路径，从桌面开始选
- 路径两边的引号、粘贴的 `file:///...` 会收干净

### 窗口上看不到版本号

版本在 exe 属性和内部常量里有，标题栏只有「设计底稿导出」。标题改为 `设计底稿导出 1.1.0`，窗口内部不改。

---

## 1.2.0

### 拖进队列成功，一点导出却是 0 / 0 / 0

业务员从 Bandizip 预览、WeTransfer 长路径或网盘未下完的位置往窗口拖文件。日志写了「加入」，导出却「完成 0、跳过 0、失败 0」，队列还不减。软件本身不联网；不稳的是源文件所在的磁盘或压缩软件临时目录。旧逻辑加入时只看当时看不看得到，导出时用另一套扫描把已经无效的路径滤成空列表，于是三个 0 且没有 `[n/m]`。

改成窗口加入只走 `prepare_sources`：清理路径 → 整包 zip 展开 / `xxx.zip\内部文件` 抽取 / rar·7z 只提示先解压 → 等待可读 → 临时、UNC、过长或重试过才拷到 `%LOCALAPPDATA%\lazysci\design-export-cache`。桌面、已解压文件夹里一次能打开的普通文件仍走原路径，不拷贝。导出时 `collect_batch_sources` 仍保留失踪项；队列有文件但收集为空，一律记失败并写日志，禁止再出现无声三个 0。

### 电脑卡、杀毒扫盘、压缩软件还没写完

拖入那一瞬间 `is_file()` 为真，真正打开时失败或读到 0 字节。加入时在约 1 秒内重试几次；中间重试过才成功的，拷到本地再进队。每个文件开始导出时再等约 2 秒。不把等待拉到 10 秒以上，以免业务员以为软件死了。

### 拖入要马上有反应

后台在解压、重试、拷缓存时，不能看起来像没拖进去。文件一放下就写「正在识别拖入的文件…」，拖入区改成「正在识别…」，先刷一帧再扫描。识别过程中再拖会提示稍后再加，关窗口也会先等识别结束。

### 不接 rar / 7z，也不强制所有文件进缓存

只用标准库 `zipfile`。大 PSD 在稳定本地路径上不拷一遍。读写走 `\\?\` 长路径，用户看到的路径不改。

### 一点开像没反应，1.2 比 1.1 更明显

单文件 exe 必须先解压再跑 Python，解压完之前任何窗口代码都执行不到。实测：`发布` 里 1.1 与 1.2 体积几乎相同（约 67.0 MB），解包完成后到主窗口约 1 秒，不是 1.2 业务代码更慢。带 Tcl 启动画面的试验包约 68.5 MB，会明显更慢，**不要**误用 `release-build` 里旧的大包当发布版。曾试验 PyInstaller Splash，已撤销；主窗口仍先 `show()`，记住设置走 `finish_startup()`。本机开发请用 `启动转换.bat`（走 `python app.py`），不要优先双击 `发布` 里的 exe。单文件多次打开会在 `%TEMP%` 留下 `_MEI*` 解压目录，多了会拖到十来秒，可运行 `清理PyInstaller临时目录.bat`。转换库预热推迟 6 秒，避免与解包抢磁盘。

### 进缓存后输出不能跟着进缓存

微信临时文件、zip 展开后源会拷到本地短名缓存。若按「队列里的新路径」推输出文件夹，会指到 `%LOCALAPPDATA%\lazysci\design-export-cache\导出结果`。输出仍按**刚拖入的原路径**判断；缓存目录本身也当成不稳定输出，记住设置不会存进去。

---

## 1.2.1

### 秒开窗口 + 准备中提示

主窗口仍先 `show()`，转换库在后台线程加载。加载期间拖入区显示「正在加载，请稍候…」，底部进度条为不确定进度并标注「准备中…」，日志写「正在加载转换组件…」和「加载完成，可以拖入文件。」。避免用户以为已就绪却点导出无反应，也避免启动时与单文件解包抢磁盘（不再固定延迟 6 秒）。

---

## 已知限制（不是这次引入的 bug）

- 「停止」不会打断正在转换的那一个大文件
- 导出是预览底稿：图层、字体、特效不保证完整；大 PSD 可能只有缩略图；INDD / CDR / Sketch 经常只能抽出预览图
- 未安装 Illustrator 时，AI 主要靠内嵌 PDF / 预览图
- 32 位 Windows 不支持
- 单文件第一次启动仍然偏慢（解压 + 杀毒扫描）

---

### 微信里发送 exe 显示问号

手机微信读不出 Windows 程序图标，exe 会显示成问号，也不利于信任。改为打包时同时生成 zip（内含 exe 和「请先读我.txt」），微信传播发 zip。真正提高 Windows「未知发布者」提示，需要购买代码签名证书，1.1.0 未做。

---

## 发布约定

- 打包：本机运行 `打包成EXE.bat`（只用 `.venv38`）
- 发给别人：只发 `发布\设计底稿导出-1.2.1\设计底稿导出.exe` 或同目录 zip
- 先保存到桌面再打开
- 不要用 Python 3.12 / PySide6 的环境打 Win7 包
